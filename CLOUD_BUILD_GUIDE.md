# بوابة الأسئلة بلس — بناء آلي في السحابة عبر GitHub Actions

بدلاً من تثبيت Android Studio و JDK و SDK على جهازك، يمكنك بناء ملف `.aab`
تلقائياً في سحابة GitHub ثم تنزيله جاهزاً.

---

## ▶️ الطريقة (3 خطوات فقط)

### 1️⃣ ارفع الكود إلى GitHub
```bash
git add .
git commit -m "add cloud build"
git push
```

### 2️⃣ شغّل الـ Workflow
1. افتح مستودعك على GitHub
2. اذهب إلى تبويب **Actions**
3. اختر **Build Android AAB** من القائمة الجانبية
4. اضغط **Run workflow** (الزر الأخضر)
5. انتظر 5-8 دقائق

### 3️⃣ نزّل ملف `.aab`
1. بعد انتهاء البناء، افتح تشغيل الـ Workflow
2. في أسفل الصفحة ستجد قسم **Artifacts**
3. اضغط على `quizgate-plus-aab` للتنزيل
4. ستُنزّل ملف مضغوط يحتوي على `app-release.aab`
5. ارفع هذا الملف إلى Google Play Console

---

## ⚙️ ما يفعله الـ Workflow تلقائياً

| الخطوة | الوصف |
|------|--------|
| 1 | يثبّت Node.js + Java + Android SDK |
| 2 | يثبّت اعتماديات المشروع |
| 3 | يبني تطبيق الويب (Vite) |
| 4 | يضيف منصة Android ويثبّت اسم الحزمة `sa.quizgate.questions` |
| 5 | يولّد مفتاح توقيع ويبني `.aab` موقّع |
| 6 | يرفع الملف كـ artifact للتنزيل |

---

## ⚠️ ملاحظة مهمة للتوقيع

المفتاح المستخدم هنا **يُولّد في كل تشغيل**، لذا التوقيع يتغيّر في كل بناء.
هذا جيد للاختبار (Internal Testing)، لكن **للنشر الرسمي على Google Play**
يلزمك مفتاح ثابت — راجع التعليقات في نهاية ملف `.github/workflows/build-android.yml`
لاستخدام keystore ثابت عبر GitHub Secrets.

---

## 🔄 النشر التلقائي المستقبلي

يمكن تطوير الـ Workflow لاحقاً لرفع الملف تلقائياً إلى Google Play
عند دمج كود جديد في فرع `main` — فقط أضف مفتاح Play Console API.